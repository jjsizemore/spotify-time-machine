# shellcheck disable=SC2148
# =============================================================================
# ENVIRONMENT CONFIGURATION EXAMPLE
# =============================================================================
# 
# This file documents all required environment variables for the application.
# 
# FILE PRECEDENCE (Next.js loads in this order, highest to lowest):
#   1. .env.local          <- USE THIS for local development (not tracked in git)
#   2. .env.development    <- Development-specific defaults
#   3. .env.production     <- Production-specific defaults
#   4. .env.ci             <- CI/CD encrypted values (dotenvx)
#   5. .env                <- Shared defaults (tracked in git, encrypted with dotenvx)
#
# FOR LOCAL DEVELOPMENT:
#   cp .env.example .env.local
#   # Then edit .env.local with your actual values
#
# FOR CI/CD SETUP:
#   See docs/how-to/github-actions-dotenvx-setup.md for complete instructions
#   1. cp .env.example .env.ci
#   2. Edit .env.ci with CI values
#   3. dotenvx encrypt -f .env.ci
#   4. Add DOTENV_PRIVATE_KEY_CI to GitHub Secrets
#
# =============================================================================

# Spotify API Configuration
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret

NEXTAUTH_SECRET=your_nextauth_secret_key_here
# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000

# Analytics Configuration
# Google Analytics 4
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Vercel Analytics (automatically enabled on Vercel)
# No configuration needed

NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com
# PostHog Analytics
NEXT_PUBLIC_POSTHOG_KEY=your_posthog_key

# Sentry Error Tracking
NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn
SENTRY_AUTH_TOKEN=your_sentry_auth_token
SENTRY_ORG=your_sentry_org
SENTRY_PROJECT=your_sentry_project
